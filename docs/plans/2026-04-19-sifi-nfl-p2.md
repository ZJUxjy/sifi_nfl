# SIFI NFL — P2 收尾实施计划

> **接力 P0+P1。** P0+P1 已让仓库恢复"绿色（编译/lint/test/全季集成）"，但留下 28 条 baseline TS 错误、2 条断言挂掉的旧测试、若干在 plan 末尾被显式 defer 的游戏规则坑。本计划 (P2) 把这些一次清掉。

**Goal:** 让 `tsc --noEmit` 真正零错；让 `npm test` 在不排除 `game.test.ts` / `team.test.ts` 的情况下全绿；补齐 `GameSim` 还缺的几条规则；治理架构里残留的隐式单例/硬编码赛季年；删掉真正没人用的老依赖。

**Architecture:** 不引入新模块、不改外层 API surface。所有改动落在 entities/types 收口、`worker/core/*` 内部、`worker/db/*` schema、`ui/stores/*` selector、`package.json` 清理。**严禁**触碰 `worker/api` 公开签名（P1 已经稳了，UI/CLI 不能再被打回去）。

**Tech Stack:** 与 P1 同（TypeScript 5.9、Vitest 4、Vite 6、Zustand 5、React 19、IndexedDB、Node ≥20）。

---

## 0. 前置说明

### 0.1 工作流

- **Branch**：复用 `fix/p0-p1-recovery`（追加 commits）；如果想分支隔离，可建 `fix/p2-baseline`，**但 PR 合并前 rebase 到 P1 之上**。
- **每个 Task 独立 commit**，`fix(...)` / `refactor(...)` / `chore(...)` 前缀。
- **TDD 适用**：`Phase B/C` 的游戏规则修复必须 RED→GREEN。`Phase A` 多为类型/字段对齐，允许直接修。`Phase D/E` 的架构 / 清理类，配合契约测试或灰度测试。
- **类型检查**：`npx tsc --noEmit`
- **单测**：`npx vitest run`（**不再 exclude** `game.test.ts` / `team.test.ts`）
- **Lint**：`npm run lint-js`

### 0.2 验收标准

- `npx tsc --noEmit` 退出码 0
- `npx vitest run`（**全量、不排除任何文件**）退出码 0
- `npm run lint-js` 退出码 0
- `rg "from 'commander'|from 'confetti-js'|from 'd3-dsv'" src` 输出为空（已无残留导入）
- `rg "Math\\.random\\(\\)" src/worker/core/game src/worker/core/draft src/worker/core/season src/worker/core/freeAgent src/worker/core/trade --type ts | wc -l` ≤ 5（剩余仅注释 / 故意）—— P1 留下的随机源治理在本 phase 收口

---

# Phase A — 编译基线闭环（28 → 0）

> 验收：`npx tsc --noEmit && echo OK` 输出 `OK`。

## A1. 删除 `Contract.options` 的 5 处幽灵字段

**Files:**
- Modify: `src/worker/api/GameEngine.ts:702`
- Modify: `src/worker/core/contract/negotiation.ts:112`
- Modify: `src/worker/core/draft/pool.ts:121, 177`
- Modify: `src/worker/core/freeAgent/market.ts:146`

**Background:** `Contract` 类型 (`src/common/types.ts:114-122`) 没有 `options` 字段；5 处构造合同对象时却写了 `options: []`。这是早期版本残留 — 删掉，没人读它。

**Steps:**

- [ ] 1. `grep -n "options:" $FILES` 复核 5 处都是 `options: []` 字面量、无外部读取
- [ ] 2. 5 处全删（保持其它字段不动）
- [ ] 3. `npx tsc --noEmit 2>&1 | grep "options"` 输出为空
- [ ] 4. Commit:
```
fix(contract): drop legacy `options: []` field absent from Contract type
```

---

## A2. 修 `team.market` 大小写不一致

**Files:**
- Modify: `src/worker/api/GameEngine.ts:749`

**Background:** `MarketSize = 'huge' | 'large' | 'medium' | 'small'`（小写，`entities.ts:3`），但 `calculateTeamFinances` 比较的是 `'Huge'/'Large'/'Medium'`，三个比较恒为 false → marketMultiplier 永远 0.8。

**Steps:**

- [ ] 1. 把 `'Huge'/'Large'/'Medium'/'Small'` 改成小写
- [ ] 2. `rg "'(Huge|Large|Medium|Small)'" src` 找其它残留并改正
- [ ] 3. `npx tsc --noEmit 2>&1 | grep "MarketSize\|calculateTeamFinances"` 为空
- [ ] 4. Commit:
```
fix(finances): compare team.market in lowercase to match MarketSize union
```

---

## A3. 修 `team.salaryCap` 与 `LeagueStructure.luxuryPayroll` 类型缺口

**Files:**
- Modify: `src/worker/api/GameEngine.ts:778`
- Modify: `src/worker/core/contract/negotiation.ts:189-199`
- 视情况：`src/common/types.ts`（`LeagueStructure` 联合类型）

**Background:**
- `Team` 上没有 `salaryCap`；`GameEngine` 写 `team.salaryCap || 180000000` —— 实际永远走默认值。
- `LeagueStructure` 是个联合类型，pyramid 子集**没有** `luxuryPayroll` 字段，`negotiation.ts` 直接读取会编译失败。

**Steps:**

- [ ] 1. `GameEngine.ts:778` 改为读 `LeagueStructure.salaryCap`：
  ```ts
  const structure = getLeagueStructure(team.region);
  capSpace: Math.max(0, ((structure as { salaryCap?: number }).salaryCap ?? 180_000_000) - payroll),
  ```
  如果 `getLeagueStructure` 不在 API 层，本任务**不做跨层调用** —— 改为读 `team.region` 后从 `@common/constants.football` 取。
- [ ] 2. `negotiation.ts:189-199` 改为：
  ```ts
  const luxury = (structure as { luxuryPayroll?: number }).luxuryPayroll;
  if (!luxury) return 0;
  // ...
  ```
  保证 pyramid 联赛分支返回 0 / 跳过奢侈税计算。
- [ ] 3. （可选）在 `types.ts` 把 `LeagueStructure` 三个变体显式 narrow 为 discriminated union，以后访问可不需 `as`
- [ ] 4. `npx tsc --noEmit 2>&1 | grep -E "salaryCap|luxuryPayroll"` 为空
- [ ] 5. Commit:
```
fix(finances): narrow LeagueStructure access for salaryCap/luxuryPayroll on pyramid leagues
```

---

## A4. 修 `Player.region` 缺失（draft pool 误读）

**Files:**
- Modify: `src/worker/core/draft/pool.ts:134-137`

**Background:** `Player` 上没有 `region` 字段（`entities.ts:164-198`），`pool.ts` 用 `player.region` 来分桶选秀池资格。应该从 `player.draft.region` 或调用方传入的 region 决定。

**Steps:**

- [ ] 1. 看 `generateDraftPool` 的调用链：调用方 (`GameEngine.generateDraftPool`) 已知 region（`getRegionTeams` 可推），可以**作为参数传入**而非从 player 读
- [ ] 2. `pool.ts:134-137` 把 `player.region` 改为函数参数 `region: Region`
- [ ] 3. `GameEngine.generateDraftPool` 传入 `'firstContinent'` / 或者按 season 阶段的 region
- [ ] 4. `npx tsc --noEmit 2>&1 | grep "pool.ts"` 为空
- [ ] 5. Commit:
```
fix(draft): drive prospect region eligibility by parameter, not non-existent player.region
```

---

## A5. `roster.ts` Position 表补 KR/PR

**Files:**
- Modify: `src/worker/core/team/roster.ts:15`

**Background:** `Position` 类型在 `types.ts:63-76` 已有 `KR`/`PR`；`roster.ts` 的 `Record<Position, number>` 配置只有 11 个位置，缺 `KR/PR` —— 编译错误。

**Steps:**

- [ ] 1. 把 `KR: 0, PR: 0` 加到配置里（兼职位置，正常球队不单独配置专职 KR/PR）
- [ ] 2. `npx tsc --noEmit 2>&1 | grep "roster.ts"` 为空
- [ ] 3. Commit:
```
fix(roster): include KR/PR slots (count 0) so Record<Position, number> is exhaustive
```

---

## A6. `generate.ts` Position narrowing

**Files:**
- Modify: `src/worker/core/player/generate.ts:211-223`

**Background:** `BASE_POSITIONS` 是 `readonly` tuple，传给参数 `Position[]` 失败；同段里 `pickWeightedRandom(...)` 返回 `Position | undefined` 又被赋值给 `Position`。

**Steps:**

- [ ] 1. 把签名改为 `readonly Position[]` 或 `(Position | undefined)[]`，按上下文决定
- [ ] 2. 对返回的 `Position | undefined` 加 `if (!pos) throw new Error(...)` 或 `?? 'OL'` 兜底
- [ ] 3. `npx tsc --noEmit 2>&1 | grep "generate.ts"` 为空
- [ ] 4. Commit:
```
fix(player/generate): narrow Position selection result and accept readonly position arrays
```

---

## A7. `worker/db` IDB schema 类型对齐

**Files:**
- Modify: `src/worker/db/Cache.ts:360-407`
- Modify: `src/worker/db/connectLeague.ts:91`

**Background:** `Cache.ts` 的 `byID` 实现把 `Player/Team/Game/...` 喂给一个 `{ id?: number }` 类型 —— 实际主键是 `pid/tid/gid/dpid`，不是 `id`。`connectLeague.ts` 的 `teamStats` storeKeyPath 类型是 number，但 schema 索引值是个 tuple，不符合 `string` 索引签名。

**Steps:**

- [ ] 1. `Cache.ts` 修 5 处：把 `byID(P, X)` 改为按真实主键访问 (`p.pid` / `t.tid` / `g.gid` / `s.gid` / `dp.dpid`)，类型签名加上正确字段
- [ ] 2. `connectLeague.ts:91` 给 `teamStats` 加显式 keyPath `['season','tid']` 并把 schema value 类型补全
- [ ] 3. `npx tsc --noEmit 2>&1 | grep -E "Cache.ts|connectLeague.ts"` 为空
- [ ] 4. Commit:
```
fix(db): align IDB schema id types with real primary keys (pid/tid/gid/dpid)
```

---

## A8. `GameSim:173` overtime 状态机 + `PenaltyInfo.type` 字段

**Files:**
- Modify: `src/worker/core/game/GameSim.ts:163-176, 320-330`
- Modify: `src/worker/core/game/penalties.ts`（`PenaltyInfo` 类型补 `type`）

**Background:**
- `simOvertime` 把 `overtimeState = 'initial'`，然后 `while (..., this.overtimeState !== 'over')`；TS 报"两个字面量永不相等"。即使忽略类型，业务上**也确实**没人会把它改回 `'over'`，加时永远跑满 10 分钟才结束 —— 隐性错误。
- `GameSim.ts:323` 读 `penalty.info.type`，但 `PenaltyInfo` 类型上没声明 `type` 字段。

**Steps:**

- [ ] 1. 把 `overtimeState` 类型扩展为 `'initial' | 'firstPossession' | 'over'`，并在 NFL OT 规则下：
  - 初次拿到球：`'initial' → 'firstPossession'`
  - 该次进攻得 TD：`= 'over'`（决胜）
  - 该次只得 FG：保持 `'firstPossession'`，对方至少要还一回合
  - 双方都还没得分 → 第二次进攻方得任意分都终结
  - **注意**：本任务**只**修类型字面量与 `'over'` 终态触发，规则细化推到 Phase C。
- [ ] 2. `PenaltyInfo` 类型加 `type: PenaltyType`（`PenaltyType` 已在 `types.football.ts`）
- [ ] 3. `npx tsc --noEmit && echo OK` 输出 `OK`（**A1-A8 全做完后**）
- [ ] 4. Commit:
```
fix(gamesim): extend overtimeState union with 'over'; PenaltyInfo gains required type field
```

### A 阶段完工检查

```bash
npx tsc --noEmit && echo PHASE_A_GREEN
```

---

# Phase B — 测试回归修复

> 验收：`npx vitest run`（**不排除任何文件**）退出码 0。

## B1. `team.test.ts` budget 期望与新生成对齐

**Files:**
- Modify: `src/test/team.test.ts:29-30`
- 视情况：`src/worker/core/team/generate.ts`（确认 budget 1.8 亿是设计还是 bug）

**Background:**
```
expected 180000000 to be 200000  // line 29 budget
```
旧测试断言 `budget=200000`，是 P0 之前的小数额版本。`team/generate.ts` 现在生成的是真实 NFL 数量级（1.8 亿）。**优先**改测试。

**Steps:**

- [ ] 1. `cat src/worker/core/team/generate.ts | grep -n "budget"`：确认设计就是大数额 ✓
- [ ] 2. 把 `expect(team.budget).toBe(200000)` 改为 `expect(team.budget).toBeGreaterThanOrEqual(50_000_000)`
- [ ] 3. `expect(team.cash).toBe(100000)` 同样改成范围断言
- [ ] 4. `npx vitest run src/test/team.test.ts`：通过
- [ ] 5. Commit:
```
test(team): assert budget/cash on realistic order-of-magnitude (post-P1 generator)
```

---

## B2. `game.test.ts` `getPlayer` undefined 崩溃 + safety→kickoff 防御

**Files:**
- Modify: `src/worker/core/game/GameSim.ts:711-718`
- Modify: `src/worker/core/game/GameSim.ts:640-644`（`doKickoffFromSafety`）

**Background:**
```
TypeError: Cannot read properties of undefined (reading 'player')
  at GameSim.getPlayer  src/worker/core/game/GameSim.ts:712
  at GameSim.doKickoff  src/worker/core/game/GameSim.ts:544
  at GameSim.doKickoffFromSafety  src/worker/core/game/GameSim.ts:642
  at GameSim.simPlay  src/worker/core/game/GameSim.ts:190
```
`safety` 后 `scoreSafety()` 已设 `awaitingKickoff = this.o`、`awaitingAfterSafety = true`，再触发 `simPlay()`，进 `awaitingAfterSafety` 分支，再 `doKickoffFromSafety` → `doKickoff` → `getPlayer(team, 'K')`。问题 ① `simPlay` 的 `if (awaitingKickoff !== undefined)` 抢在 `awaitingAfterSafety` 之前，先把 `awaitingKickoff` 消耗掉，导致后面 `doKickoffFromSafety` 时再 `doKickoff` 时 `team` 编号被翻转、`this.team[?]` 越界 —— 实际触发原因要现场打印确认。

**Steps:**

- [ ] 1. **失败测试存在** —— `src/test/game.test.ts > should simulate a complete game` 已经天然 RED
- [ ] 2. 修 `getPlayer`：访问前断言 team 在范围内
  ```ts
  getPlayer(team: TeamNum, pos: string): PlayerGameSim {
    const t = this.team[team];
    if (!t) {
      throw new Error(`getPlayer called with invalid team ${team} (o=${this.o}, d=${this.d})`);
    }
    const players = t.player.filter(p => p.pos === pos);
    return players[0] || t.player[0];
  }
  ```
- [ ] 3. 修 `doKickoffFromSafety` —— 不要在 `simPlay` 里二次进入 `doKickoff`：把 `awaitingAfterSafety` 分支的逻辑写完整（让 safety 后的自由踢 = 一次专门的 kick play，**不**复用 `awaitingKickoff` 路径），或者在 `simPlay` 里把 `awaitingAfterSafety` 的检查放在 `awaitingKickoff` 之前
- [ ] 4. `npx vitest run src/test/game.test.ts`：should simulate a complete game 通过
- [ ] 5. `npx vitest run`：**全量 0 fail**
- [ ] 6. Commit:
```
fix(gamesim): guard getPlayer against invalid team; fix safety→free-kick state machine
```

### B 阶段完工检查

```bash
npx vitest run && echo PHASE_B_GREEN
```

---

# Phase C — GameSim 规则补全

> 每个 task 独立测试 + commit。完工后 `getResult()` 在 1000 局快速回归里方差 / 极值更接近 NFL 历史区间（不强制定量验收）。

## C1. `passRushers` 空数组 NaN 防御

**Files:**
- Test: `src/test/gamesim.passRushers.test.ts`
- Modify: `src/worker/core/game/GameSim.ts`（`getPassRushers` / `doPass` sack 概率分母）

**Background:** plan 末尾 P2 列表的 B6：当 `defenders.filter(...)` 为空（极端阵容），后续 `sum / passRushers.length` 出 NaN，扩散到 `Math.random() < NaN` 全 false → 永不 sack → toGo 漂走。

**Steps:**

- [ ] 1. 写 RED：构造一支阵容里没 DL/LB/CB（应不可能但要防），断言 `doPass` 不抛、不出 NaN
- [ ] 2. 加 guard：`if (passRushers.length === 0) return defaultRushers;`
- [ ] 3. GREEN + commit:
```
fix(gamesim): default pass-rusher set when defense has none, avoid NaN sack probability
```

---

## C2. 2-point conversion

**Files:**
- Test: `src/test/gamesim.twoPoint.test.ts`
- Modify: `src/worker/core/game/GameSim.ts`（`doExtraPoint`）

**Background:** 当前 `awaitingAfterTouchdown` 永远走 XP，缺 2PT。设计：`scoreTouchdown` 后由 AI 选择 — 比赛后段落后则更倾向 2PT。

**Steps:**

- [ ] 1. RED：构造比赛 4Q clock 1:00、得分差为 -2，断言 `doExtraPoint` 后**会**触发 2PT 尝试（部分 seed）
- [ ] 2. 实现 `decideXpOrTwo()` 简化策略
- [ ] 3. 实现 `doTwoPointConversion()`（成功 +2，失败 0），不影响 `awaitingKickoff` 流程
- [ ] 4. GREEN + commit:
```
feat(gamesim): two-point conversion option after TD with situational AI
```

---

## C3. 抄截/掉球回攻 TD（pickSix / fumble-six）

**Files:**
- Test: `src/test/gamesim.pickSix.test.ts`
- Modify: `src/worker/core/game/GameSim.ts:doPass`（int 分支） & `doRun`（fumble 分支）

**Background:** 当前 `int`/`fumble` 转换球权后默认从拿球点开始，没有"防守方回攻得分"概率。

**Steps:**

- [ ] 1. RED：mock random 让一次 int 走到 100% 回攻；断言 defender 拿到 1 个 `defTD`，对手 `pts += 6`
- [ ] 2. 在 int/fumble 的 turnover 分支加：`returnYards = ...`；如果 `newScrimmage >= 100`，调用 `scoreDefensiveTouchdown(defender)`
- [ ] 3. GREEN + commit:
```
feat(gamesim): support defensive return touchdowns on interceptions and fumbles
```

---

## C4. 防守得分（blocked kick TD / safety 后的 score）

**Files:**
- Modify: `src/worker/core/game/GameSim.ts:doFieldGoal/doPunt`

**Background:** FG 被 block 后球地上随机回收，可能形成防守 TD；punt 同理。

**Steps:**

- [ ] 1. RED：mock 某次 FG block 概率到 1，断言出现 `blockedKickTD` 事件 + 防守方得分
- [ ] 2. `doFieldGoal` `block` 分支加 70% 球权交换、30% TD（占位概率）
- [ ] 3. GREEN + commit:
```
feat(gamesim): defensive scores on blocked field goals and punts
```

---

## C5. 加时规则收敛（NFL modern OT）

**Files:**
- Modify: `src/worker/core/game/GameSim.ts:simOvertime`
- Test: `src/test/gamesim.overtime.test.ts`

**Background:** A8 已经把 `overtimeState` union 扩好；本 task 把规则跑通：第一队 TD 即胜，FG 后让对方一回合，第二队任意得分 vs 平局再来 OT。

**Steps:**

- [ ] 1. RED：构造 4Q 收尾平局，模拟一段 OT，断言：① 第一队 TD → 立即结束；② 第一队 FG → 对方有一回合
- [ ] 2. 实现状态机推进
- [ ] 3. GREEN + commit:
```
feat(gamesim): modern NFL overtime rules — TD wins, FG concedes a possession
```

---

## C6. 伤病实际影响出战与能力

**Files:**
- Modify: `src/worker/core/game/injuries.ts` 或 `GameSim.getStarter`
- Test: `src/test/gamesim.injury.test.ts`

**Background:** 当前伤病只写 `injury.ovr` 字段、不阻挡上场；阵容选择仍按 ratings.ovr 算。

**Steps:**

- [ ] 1. RED：给主力 QB 灌 `injury.gamesRemaining = 5, ovr = -50`，断言下一场首发 QB 不是他 / 或上场但 sack 概率显著上升
- [ ] 2. 在 `getStarter` / `getDepthChart` 阶段对 `injury.gamesRemaining > 0` 的球员降权或剔除
- [ ] 3. GREEN + commit:
```
feat(gamesim): injuries actually demote players in depth chart selection
```

---

# Phase D — 架构治理

## D1. `StatsManager` 不再做隐式单例

**Files:**
- Modify: `src/worker/core/stats/StatsManager.ts`
- Modify: 所有 import 它的位置（grep）
- Test: `src/test/stats.isolation.test.ts`

**Background:** `StatsManager` 用模块级闭包累计，多场测试串扰。改为类 / 由 `GameEngine` 注入实例。

**Steps:**

- [ ] 1. RED：连续两场 `finalizeGame`，断言第二场的 `pssYds` 不会包含第一场的累积
- [ ] 2. 把模块级 state 收进 class；`GameEngine` 持有一个实例
- [ ] 3. GREEN + commit:
```
refactor(stats): turn StatsManager into instance owned by GameEngine to remove cross-game state
```

---

## D2. `storage.ts` 不再硬编码 `get(2025)`

**Files:**
- Modify: `src/worker/api/storage.ts`（或 `worker/db/Cache.ts`）

**Background:** plan 末尾点名：`storage.ts` 在某处 `cache.get(2025)`，跨年读不到当季数据。

**Steps:**

- [ ] 1. `rg "get\\(2025\\)|2025\\b" src/worker/db src/worker/api/storage*`：定位
- [ ] 2. 改为接受 `season: number` 参数，调用方传 `engine.getState().season`
- [ ] 3. Commit:
```
fix(storage): parameterize season for cache reads instead of hardcoded 2025
```

---

## D3. `gameStore` re-render 治理（细粒度 selector）

**Files:**
- Modify: `src/ui/stores/gameStore.ts`
- Modify: 高频订阅组件（`StatsView`, `ScheduleView`, `TeamRoster` 等）

**Background:** 当前 `useGameStore()` 整体订阅 → 每次 simWeek 全树 re-render。改为 selector。

**Steps:**

- [ ] 1. 给 store 加 selector helper：`useTeams()`, `usePlayers()`, `useStandings(region)` 等
- [ ] 2. 把组件里 `const { teams, players } = useGameStore()` 替换为 selector hook
- [ ] 3. （可选）测一个 React Profiler 截图当作前后对比
- [ ] 4. Commit:
```
perf(ui/store): add fine-grained selector hooks to cut needless re-renders
```

---

## D4. `ScheduleView` useMemo deps

**Files:**
- Modify: `src/ui/components/ScheduleView.tsx`

**Background:** `useMemo(() => ..., [teams])` 但内部还引用 `season/week` —— 数据切片不更新。

**Steps:**

- [ ] 1. 全文件检查 `useMemo`/`useCallback` 的 deps，按 `react-hooks/exhaustive-deps` 提示补全
- [ ] 2. `npm run lint-js`：本文件 0 warning
- [ ] 3. Commit:
```
fix(ui/schedule): satisfy exhaustive-deps on useMemo
```

---

# Phase E — 清理升级

## E1. `generate.ts` 与 `ovr.ts` 权重收口

**Files:**
- Modify: `src/worker/core/player/ovr.ts`
- Modify: `src/worker/core/player/generate.ts`

**Background:** 两边维护独立的权重表，长期会漂；以 `ovr.ts` 为权威，`generate.ts` import 之，避免重复维护。

**Steps:**

- [ ] 1. 抽出共享常量 `POSITION_WEIGHTS` 到 `ovr.ts`
- [ ] 2. `generate.ts` 删除本地副本，import
- [ ] 3. 跑一份 1000 球员生成 + 重算 ovr 的回归测试，断言两边一致
- [ ] 4. Commit:
```
refactor(player): single source of truth for per-position rating weights
```

---

## E2. 老依赖清理（`confetti-js` 等）

**Files:**
- Modify: `package.json`

**Background:** Task 24 已删了一批；P2 收尾再扫一遍 `import` 实际用到的。

**Steps:**

- [ ] 1. `rg "from '(confetti-js|d3-dsv|...)'" src --type ts --type tsx`：列出
- [ ] 2. 对每个**真**未用的依赖：`npm uninstall $pkg`
- [ ] 3. `npx tsc --noEmit && npm test`：全绿
- [ ] 4. Commit:
```
chore(deps): drop unused front-end deps post P1 cleanup
```

---

# 完工总检查

- [ ] `npx tsc --noEmit && echo OK`
- [ ] `npx vitest run && echo OK`（**不再排除任何文件**）
- [ ] `npm run lint-js && echo OK`
- [ ] `git log --oneline fix/p0-p1-recovery | head -50` 看到 P2 的所有 commit 干净独立

---

# 执行交接

**两种执行选项（同 P0+P1）：**

1. **Subagent-Driven（推荐）** — 每个 task 派一个新 subagent，主对话只做 review + 推进
2. **Inline Execution** — 当前会话内连续执行，每完成 phase 做 checkpoint review

**也可以先只跑 Phase A+B（最小：编译 + 测试 0 错），Phase C/D/E 之后再说。**
