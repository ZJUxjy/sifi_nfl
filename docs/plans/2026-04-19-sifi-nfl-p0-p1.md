# SIFI NFL — P0 + P1 修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复阻塞编译的类型错误、GameSim 规则 bug、赛程/季后赛/休赛期的实现错误、存档安全漏洞，并初步收敛 worker/api 边界与 seedable 随机源，让项目重新进入"可编译 + 可信任"的工程基线。

**Architecture:** 分两阶段。**P0**：让仓库立即恢复"绿色"——`tsc`、`eslint`、`vitest` 都能跑，最致命的游戏规则 bug 修掉，存档攻击面收紧。**P1**：把暴露的双轨实现（赛程/v1 v2 季节系统）做单一权威源切换，加 seed 随机、补存档 schema、收口 worker/api、把 GameSim 真正搬到 Worker。

**Tech Stack:** TypeScript 5.9、Vitest 4、Vite 6、Zustand 5、React 19、IndexedDB（@dumbmatter/idb）、Node ≥20。

---

## 0. 前置说明

### 0.1 工作流

- **Branch**：`fix/p0-p1-recovery`
- **每个 Task 是独立 commit**。一个 Task 内 TDD 步骤聚合到 1 个 commit；纯配置/纯删除可独立 commit。
- **TDD 适用范围**：游戏逻辑、引擎、纯函数。**不强制**：tsconfig/eslint/tools 脚本、目录改动、IDE 配置。
- **测试命令**：`npx vitest run <path>`（单文件）/ `npm test`（全部）。
- **类型检查**：`npx tsc --noEmit`（避免 `npm run lint` 在 ESLint 配置补齐前失败）。

### 0.2 验收标准

- `npx tsc --noEmit` 退出码 0
- `npm test` 通过，并新增 ≥ 25 个针对本计划修复点的回归测试
- `npm run lint-js` 在新增 ESLint config 后退出码 0
- 列在本计划"成功度量"小节的所有问题已可验证消除

### 0.3 命名约定

- 新测试文件：`src/test/<scope>.<topic>.test.ts`（例：`gamesim.safety.test.ts`、`saveManager.security.test.ts`）
- 新增工具：放在 `tools/`（不存在则现建）

---

# Phase P0：恢复编译 + 阻断致命 bug

---

## Task 1: 修复 `StatsManager` 错误的类型来源（TS2305）

**Files:**
- Modify: `src/worker/core/stats/StatsManager.ts:6`

**Background:** `import type { Player, Team } from '@common/types'` 实际上 `@common/types` 没导出这两个类型，正确来源是 `@common/entities`。`tsc` 报 TS2305。

- [ ] **Step 1: 复现编译错误**

Run: `npx tsc --noEmit 2>&1 | grep StatsManager`
Expected: 出现 `error TS2305: Module '"@common/types"' has no exported member 'Player'.` 类似输出。

- [ ] **Step 2: 修正 import**

```ts
import type { Player, Team } from '@common/entities';
```

- [ ] **Step 3: 验证错误消失**

Run: `npx tsc --noEmit 2>&1 | grep StatsManager`
Expected: 无 `StatsManager.ts` 的错误。

- [ ] **Step 4: Commit**

```bash
git add src/worker/core/stats/StatsManager.ts
git commit -m "fix(stats): import Player/Team from entities, not types"
```

---

## Task 2: 解除 `@common` barrel 同名符号冲突（TS2308）

**Files:**
- Modify: `src/common/index.ts`

**Background:** `export *` 同时聚合 `types` 与 `types.football`，`Player`、`Contract`、`TeamStats` 等同名导出冲突。最小修复：保留 `entities` 为业务实体来源；从 `types` / `types.football` 改为显式 re-export，把同名符号统一指向单一来源。

- [ ] **Step 1: 复现冲突**

Run: `npx tsc --noEmit 2>&1 | grep -E "TS2308|common/index"`
Expected: 多条 `TS2308 ... has already exported a member named 'Player'/'Contract'/...`

- [ ] **Step 2: 改为显式 re-export，避免同名冲突**

把 `src/common/index.ts` 改为：

```ts
// 实体（业务对象）的唯一来源
export * from './entities';

// 通用基础类型（去掉与 entities/types.football 重复的符号）
export type {
  Region,
  Phase,
  Position,
  PlayerStats,
  TeamStats as PersistedTeamStats,
  DraftPick,
} from './types';

// Football 模拟内部类型（不暴露与 entities 同名的 Player/Contract）
export type {
  PlayType,
  PenaltyType,
  InjuryType,
} from './types.football';

export * from './constants';
export * from './constants.football';
export * from './utils';
export * from './random';
export * from './names';
```

> **Note:** 上面的具体导出名以仓库现有声明为准。执行时先 `grep "^export"` 两个 types 文件，把真实导出名替换进来；目标是**每个符号只出现一次**。

- [ ] **Step 3: 验证 TS2308 消失**

Run: `npx tsc --noEmit 2>&1 | grep TS2308 | wc -l`
Expected: `0`

- [ ] **Step 4: Commit**

```bash
git add src/common/index.ts
git commit -m "fix(common): replace barrel star-export with explicit re-exports to remove duplicate symbol errors"
```

---

## Task 3: 统一 `DraftPick` 类型来源

**Files:**
- Modify: `src/worker/api/types.ts`（删除本地 `DraftPick` 重定义）
- Modify: `src/worker/api/GameEngine.ts`（使用 `@common/types` 的 `DraftPick`）

**Background:** `common/types.DraftPick` 与 `worker/api/types.DraftPick` 两套定义、`season` 字段不兼容。统一使用 `@common/types.DraftPick`，API 层不再重定义。

- [ ] **Step 1: 复现冲突**

Run: `npx tsc --noEmit 2>&1 | grep -i draftpick`
Expected: 出现 `Type 'DraftPick' is not assignable to type 'DraftPick'` 等。

- [ ] **Step 2: 删除 API 层重复定义**

在 `src/worker/api/types.ts` 中：
- 删除本地 `export type DraftPick = { ... }`
- 添加：`export type { DraftPick } from '@common/types';`

- [ ] **Step 3: 验证错误消失**

Run: `npx tsc --noEmit 2>&1 | grep -i draftpick`
Expected: 无输出。

- [ ] **Step 4: Commit**

```bash
git add src/worker/api/types.ts src/worker/api/GameEngine.ts
git commit -m "refactor(api): single source of truth for DraftPick (common/types)"
```

---

## Task 4: 让 `npx tsc --noEmit` 完整通过

**Files:**
- Modify: 视上面任务后剩余报错

**Background:** Task 1–3 之后运行一次 `tsc`，把残余报错（如 `as any`、字段缺失、`as DraftPick` 等）逐项消化。这一步**不是**重构，是收尾。

- [ ] **Step 1: 跑完整 tsc**

Run: `npx tsc --noEmit 2>&1 | tee /tmp/tsc.log; echo "exit=$?"`
Expected: 列出所有剩余错误，最终 `exit=0`（如未通过则继续修）。

- [ ] **Step 2: 逐项修复**

策略：
- 缺字段 → 补字段或改用 `Pick<>` / `Omit<>`
- 老 `as any` → 改 `as unknown as <T>` 并加 `// FIXME: typed model in P1-9`
- 不存在的 import 路径 → 修正

- [ ] **Step 3: 最终验证**

Run: `npx tsc --noEmit && echo OK`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "fix(types): close remaining tsc errors after barrel/draftpick cleanup"
```

---

## Task 5: 补 ESLint flat config + 修复 `package.json` 路径

**Files:**
- Create: `eslint.config.js`
- Modify: `package.json`（如 `tools/pre-test.ts` 缺失）
- Create（如缺）: `tools/pre-test.ts`

**Background:** 仓库根目录无 `eslint.config.*`，`npm run lint-js` 必失败。同时 `tools/` 目录缺失，`pre-test` 脚本指向不存在的文件。

- [ ] **Step 1: 创建最小 flat config**

`eslint.config.js`:

```js
import js from '@eslint/js';
import tsParser from '@typescript-eslint/parser';
import tsPlugin from '@typescript-eslint/eslint-plugin';
import reactHooks from 'eslint-plugin-react-hooks';

export default [
  js.configs.recommended,
  {
    files: ['src/**/*.{ts,tsx}', 'tools/**/*.ts'],
    languageOptions: {
      parser: tsParser,
      parserOptions: { ecmaVersion: 2022, sourceType: 'module' },
    },
    plugins: {
      '@typescript-eslint': tsPlugin,
      'react-hooks': reactHooks,
    },
    rules: {
      ...tsPlugin.configs.recommended.rules,
      'react-hooks/rules-of-hooks': 'error',
      'react-hooks/exhaustive-deps': 'warn',
      '@typescript-eslint/no-floating-promises': 'off',
      '@typescript-eslint/no-explicit-any': 'warn',
      'no-empty': ['error', { allowEmptyCatch: true }],
    },
    ignores: ['dist/**', 'build/**', 'node_modules/**'],
  },
];
```

- [ ] **Step 2: 安装缺失依赖**

Run: `npm i -D @eslint/js @typescript-eslint/parser @typescript-eslint/eslint-plugin eslint-plugin-react-hooks`
Expected: 安装成功，`package.json` `devDependencies` 增加四项。

- [ ] **Step 3: 创建/恢复 tools/pre-test.ts**

如果不存在，创建 `tools/pre-test.ts`：

```ts
// 占位：之前用于生成测试夹具，目前作为 no-op 保证 npm scripts 不破。
// 若后续需要重新生成 fixtures，再在此实现。
console.log('[pre-test] no-op');
```

- [ ] **Step 4: 验证脚本**

Run: `npm run lint-js && echo OK`
Expected: 输出 `OK` 或仅 warning。

- [ ] **Step 5: Commit**

```bash
git add eslint.config.js tools/pre-test.ts package.json package-lock.json
git commit -m "chore(lint): add eslint flat config and restore tools/pre-test.ts no-op"
```

---

## Task 6: 修 GameSim `timeRemaining` 计算（用全场剩余时间）

**Files:**
- Test: `src/test/gamesim.timeRemaining.test.ts`（新建）
- Modify: `src/worker/core/game/GameSim.ts:203-206`

**Background:** `selectPlayType` 里 `timeRemaining = this.clock + (numPeriods - Math.floor(this.clock / quarterLength) - 1) * quarterLength`。`Math.floor(clock / quarterLength)` 在 `clock <= quarterLength` 时永远是 0 或 1，这是错误的"已过节数"推断。正确语义应为 **当前节数**（`this.period` / `this.quarter`，以仓库实际字段为准）。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/gamesim.timeRemaining.test.ts
import { describe, it, expect } from 'vitest';
import { computeTimeRemaining } from '@worker/core/game/GameSim';

describe('GameSim time remaining', () => {
  it('returns full game minutes left at start of Q1', () => {
    expect(computeTimeRemaining({ clock: 15, quarter: 1, numPeriods: 4, quarterLength: 15 }))
      .toBe(60);
  });

  it('returns 15 minutes left in Q4 with full clock', () => {
    expect(computeTimeRemaining({ clock: 15, quarter: 4, numPeriods: 4, quarterLength: 15 }))
      .toBe(15);
  });

  it('returns 1 minute left in Q4 with 1 minute on clock', () => {
    expect(computeTimeRemaining({ clock: 1, quarter: 4, numPeriods: 4, quarterLength: 15 }))
      .toBe(1);
  });

  it('returns 31 minutes left at start of Q3', () => {
    expect(computeTimeRemaining({ clock: 15, quarter: 3, numPeriods: 4, quarterLength: 15 }))
      .toBe(31);
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npx vitest run src/test/gamesim.timeRemaining.test.ts`
Expected: FAIL（`computeTimeRemaining` is not a function 或返回值不对）。

- [ ] **Step 3: 抽出纯函数并修正逻辑**

在 `src/worker/core/game/GameSim.ts` 新增导出（放在 class 外部最佳）：

```ts
export function computeTimeRemaining(args: {
  clock: number;
  quarter: number;
  numPeriods: number;
  quarterLength: number;
}): number {
  const { clock, quarter, numPeriods, quarterLength } = args;
  const fullPeriodsLeft = Math.max(0, numPeriods - quarter);
  return clock + fullPeriodsLeft * quarterLength;
}
```

并把 `selectPlayType` 内的计算替换为：

```ts
const timeRemaining = computeTimeRemaining({
  clock: this.clock,
  quarter: this.quarter,           // 以仓库实际字段名为准（period / quarter）
  numPeriods: this.numPeriods,
  quarterLength: this.quarterLength,
});
```

> **Note:** 如果 class 字段名不是 `quarter`，先 `grep "this\\.quarter\\|this\\.period" src/worker/core/game/GameSim.ts` 确认；保持本任务**不重命名**字段。

- [ ] **Step 4: 跑测试，确认通过**

Run: `npx vitest run src/test/gamesim.timeRemaining.test.ts`
Expected: PASS（4 cases）。

- [ ] **Step 5: 跑全量测试，确保未引入回归**

Run: `npm test`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add src/test/gamesim.timeRemaining.test.ts src/worker/core/game/GameSim.ts
git commit -m "fix(gamesim): compute time remaining from current quarter, not from clock arithmetic"
```

---

## Task 7: 修 `doPass`/`doRun` 完成传球或冲球时漏记 safety

**Files:**
- Test: `src/test/gamesim.safety.test.ts`（新建）
- Modify: `src/worker/core/game/GameSim.ts:335-368`（`doPass` 完成分支）
- Modify: `src/worker/core/game/GameSim.ts:389-417`（`doRun`）

**Background:** sack 分支已经正确 `if (safety) scoreSafety()`（289-291 行），但 `doPass` 完成传球后的 `safety = scrimmage <= 0` 与 `doRun` 的同名变量都**只记日志、不记 2 分、不踢自由球**。`updateState` 在 `scrimmage<=0` 时直接 `return`，整条链断裂。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/gamesim.safety.test.ts
import { describe, it, expect, vi } from 'vitest';
import { GameSim } from '@worker/core/game/GameSim';

function makeGameForSafety(playKind: 'pass' | 'run') {
  // 构造一场进攻方在自家 2 码线开球的场景，让一次大码数损失把球推过端区。
  // 真正的工厂在 src/test/helpers/makeGame.ts（见 Task 12 前置 helper），此处暂内联。
  // 关键：seed 固定后，第一次 play 必须出 sack 之外的进攻 + 大量负码 / 进入端区。
  // ...
}

describe('GameSim safety scoring', () => {
  it('awards 2 points to defense and triggers free kick when offense is tackled in their own end zone on a run', () => {
    const sim = makeGameForSafety('run');
    const beforeDefPts = sim.team[sim.d].stat.pts;
    sim.doRun();
    const afterDefPts = sim.team[sim.d].stat.pts;
    expect(afterDefPts - beforeDefPts).toBe(2);
    expect(sim.lastEvent?.type).toBe('safety');
  });

  it('awards 2 points to defense on a completed pass driven back into own end zone', () => {
    const sim = makeGameForSafety('pass');
    const beforeDefPts = sim.team[sim.d].stat.pts;
    sim.doPass();
    const afterDefPts = sim.team[sim.d].stat.pts;
    expect(afterDefPts - beforeDefPts).toBe(2);
  });
});
```

> **Note:** 该测试依赖能从外部观察 `lastEvent`。如果当前没有该公开字段，先在本任务中加一个最小可观察接口（例如 `getLastEvent(): GameEvent | null`），不要改其它行为。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/gamesim.safety.test.ts`
Expected: FAIL（`afterDefPts - beforeDefPts` 为 0）。

- [ ] **Step 3: 修 `doPass` 完成分支**

在 `src/worker/core/game/GameSim.ts` 约 364–368：

```ts
    if (td) {
      this.recordStat(this.o, qb, 'pssTD');
      this.recordStat(this.o, target, 'recTD');
      this.scoreTouchdown();
    } else if (safety) {
      this.scoreSafety();
    }
```

- [ ] **Step 4: 修 `doRun` 完成分支**

约 414–417：

```ts
    if (td) {
      this.recordStat(this.o, rb, 'rusTD');
      this.scoreTouchdown();
    } else if (safety) {
      this.scoreSafety();
    }
```

- [ ] **Step 5: 跑测试，全绿**

Run: `npx vitest run src/test/gamesim.safety.test.ts && npm test`
Expected: 都通过。

- [ ] **Step 6: Commit**

```bash
git add src/test/gamesim.safety.test.ts src/worker/core/game/GameSim.ts
git commit -m "fix(gamesim): score safety on pass/run when ball is downed in offense's own end zone"
```

---

## Task 8: 修 4 档传球未完成不交换球权（turnover-on-downs）

**Files:**
- Test: `src/test/gamesim.turnoverOnDowns.test.ts`（新建）
- Modify: `src/worker/core/game/GameSim.ts:301-328`

**Background:** 第四档传球未完成时只 `down++`，没判 `down > NUM_DOWNS`，球权不会被交换。NFL 必须 turnover-on-downs。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/gamesim.turnoverOnDowns.test.ts
import { describe, it, expect } from 'vitest';
import { GameSim } from '@worker/core/game/GameSim';

describe('GameSim turnover on downs', () => {
  it('changes possession when 4th down pass is incomplete', () => {
    const sim = /* helper that puts the offense in 4th & 10 mid-field */;
    const offBefore = sim.o;
    // 强制 incomplete：mock completionProb < 0
    vi.spyOn(Math, 'random').mockReturnValue(0.99);
    sim.doPass();
    expect(sim.o).not.toBe(offBefore);
    expect(sim.down).toBe(1);
    expect(sim.toGo).toBe(10);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/gamesim.turnoverOnDowns.test.ts`
Expected: FAIL（球权未变）。

- [ ] **Step 3: 修复未完成分支**

把 `src/worker/core/game/GameSim.ts:326-328` 改为：

```ts
    this.down++;
    this.isClockRunning = false;
    if (this.down > 4) {
      this.possessionChange();
    }
    return;
```

> **Note:** 若仓库已有 `NUM_DOWNS` 常量，使用之；若 `possessionChange()` 函数名不同，按实际名调用（grep `possessionChange\\|turnover` 确认）。

- [ ] **Step 4: 验证测试通过**

Run: `npx vitest run src/test/gamesim.turnoverOnDowns.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/gamesim.turnoverOnDowns.test.ts src/worker/core/game/GameSim.ts
git commit -m "fix(gamesim): trigger turnover on downs when 4th down pass is incomplete"
```

---

## Task 9: 修罚球默认 `firstDown = true` 错误

**Files:**
- Test: `src/test/penalties.firstDown.test.ts`（新建）
- Modify: `src/worker/core/game/penalties.ts:310-344`

**Background:** `applyPenalty` 防守犯规分支（`else { ... }`）末尾**无条件** `firstDown = true`，把所有 5 码越位、防守延误等都送一档。正确：只有 `penalty.info.automaticFirstDown === true` 时才一档；其它由原始 down/distance 推得。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/penalties.firstDown.test.ts
import { describe, it, expect } from 'vitest';
import { applyPenalty, type GamePenalty } from '@worker/core/game/penalties';

const offside5: GamePenalty = {
  spotYards: undefined,
  info: {
    type: 'offside',
    yards: 5,
    isOffensive: false,
    isSpotFoul: false,
    isDeadBall: true,
    automaticFirstDown: false,
  },
} as any;

const dpi: GamePenalty = {
  spotYards: 18,
  info: {
    type: 'defensivePassInterference',
    yards: 0,
    isOffensive: false,
    isSpotFoul: true,
    isDeadBall: false,
    automaticFirstDown: true,
  },
} as any;

describe('applyPenalty firstDown', () => {
  it('does not grant first down on a 5-yard offside when toGo is greater than penalty yards', () => {
    const result = applyPenalty(offside5, /*scrimmage=*/ 50, /*toGo=*/ 10);
    expect(result.firstDown).toBe(false);
  });

  it('grants first down when defensive penalty yards are enough to make the line to gain', () => {
    const result = applyPenalty(offside5, /*scrimmage=*/ 50, /*toGo=*/ 4);
    expect(result.firstDown).toBe(true);
  });

  it('always grants first down on automatic-first-down penalty (DPI)', () => {
    const result = applyPenalty(dpi, /*scrimmage=*/ 30, /*toGo=*/ 10);
    expect(result.firstDown).toBe(true);
    expect(result.newScrimmage).toBe(48);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/penalties.firstDown.test.ts`
Expected: FAIL（第一条期望 false 但实际 true）。

- [ ] **Step 3: 修复**

把 `src/worker/core/game/penalties.ts` 防守分支重写为：

```ts
  } else {
    if (penalty.info.isSpotFoul && penalty.spotYards !== undefined) {
      newScrimmage = Math.min(99, scrimmage + penalty.spotYards);
    } else {
      newScrimmage = Math.min(99, scrimmage + yards);
    }
    const yardsAdvanced = newScrimmage - scrimmage;
    firstDown = penalty.info.automaticFirstDown === true || yardsAdvanced >= toGo;
    if (!firstDown) {
      newToGo = Math.max(1, toGo - yardsAdvanced);
    } else {
      newToGo = 10;
    }
  }
```

> **Note:** DPI 特例（336-341 行）的 `firstDown = true` 保留——DPI 在 `info.automaticFirstDown` 已为 true，可不再重复设置；保留此行可作为冗余防御。

- [ ] **Step 4: 跑测试**

Run: `npx vitest run src/test/penalties.firstDown.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/penalties.firstDown.test.ts src/worker/core/game/penalties.ts
git commit -m "fix(penalties): grant first down only when automaticFirstDown or yards advanced reach toGo"
```

---

## Task 10: 修选秀顺位实际未按战绩反序（pool.ts won:0/lost:0）

**Files:**
- Test: `src/test/draft.order.test.ts`（新建）
- Modify: `src/worker/core/draft/pool.ts:72-95`

**Background:** `generateDraftPicks` 调用 `calculateDraftOrder` 时把 `won:0, lost:0` 硬塞进去，导致全队胜率相同，顺位**接近随机**。应该接受真实战绩并传入。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/draft.order.test.ts
import { describe, it, expect } from 'vitest';
import { generateDraftPicks } from '@worker/core/draft/pool';

describe('generateDraftPicks', () => {
  it('orders first round by ascending win pct (worst team picks first)', () => {
    const teams = [
      { tid: 1, region: 'firstContinent' as const, won: 14, lost: 3 },
      { tid: 2, region: 'firstContinent' as const, won: 3, lost: 14 },
      { tid: 3, region: 'firstContinent' as const, won: 8, lost: 9 },
    ];
    const picks = generateDraftPicks(teams, 2026, 7);
    const round1 = picks.filter(p => p.round === 1);
    expect(round1.map(p => p.tid)).toEqual([2, 3, 1]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/draft.order.test.ts`
Expected: FAIL（顺序与战绩无关）。

- [ ] **Step 3: 修签名 + 不再丢弃战绩**

把 `src/worker/core/draft/pool.ts:72-95` 改为：

```ts
export function generateDraftPicks(
  teams: { tid: number; region: Region; won: number; lost: number }[],
  season: number,
  numRounds: number = 7
): DraftPick[] {
  const picks: DraftPick[] = [];
  const order = calculateDraftOrder(teams, season);

  let dpid = season * 1000 + 1;
  for (let round = 1; round <= numRounds; round++) {
    for (let i = 0; i < order.length; i++) {
      picks.push({
        dpid: dpid++,
        tid: order[i],
        originalTid: order[i],
        round,
        pick: i + 1,
        season,
      });
    }
  }
  return picks;
}
```

> **Note:** 原 `dpid` 用全局递增 `1`，跨赛季会冲突。改为 `season * 1000 + 1` 起始（最多 999 队/赛季）以避免重复。所有调用方（grep `generateDraftPicks`）都需要传入 `won/lost`。

- [ ] **Step 4: 修调用方**

Run: `rg "generateDraftPicks\\(" src --type ts`
对每个调用点补 `won`、`lost` 字段。

- [ ] **Step 5: 验证测试**

Run: `npx vitest run src/test/draft.order.test.ts && npm test`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git add src/test/draft.order.test.ts src/worker/core/draft/pool.ts $(rg -l 'generateDraftPicks\\(' src --type ts)
git commit -m "fix(draft): generateDraftPicks must use real win/loss records, not zeros"
```

---

## Task 11: 修 `saveManager.loadGame/deleteSave` 路径穿越

**Files:**
- Test: `src/test/saveManager.security.test.ts`（新建）
- Modify: `src/cli/saveManager.ts`

**Background:** `loadGame(id)` 直接 `path.join(SAVES_DIR, ${id}.json)`，`id = "../../etc/passwd"` 即可逃逸。`deleteSave` 同样有问题。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/saveManager.security.test.ts
import { describe, it, expect } from 'vitest';
import { loadGame, deleteSave } from '../cli/saveManager';

describe('saveManager id sanitization', () => {
  it('rejects ids containing path separators', () => {
    expect(() => loadGame('../../etc/passwd')).toThrow(/invalid save id/i);
    expect(() => deleteSave('../foo')).toThrow(/invalid save id/i);
  });

  it('rejects empty / dotted ids', () => {
    expect(() => loadGame('')).toThrow();
    expect(() => loadGame('.')).toThrow();
    expect(() => loadGame('..')).toThrow();
  });

  it('accepts valid id format save_<digits>', () => {
    expect(() => loadGame('save_1700000000000')).not.toThrow();
    // 文件不存在仍可返回 null，关键是不抛 invalid
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/saveManager.security.test.ts`
Expected: FAIL（不抛错）。

- [ ] **Step 3: 加白名单校验**

在 `src/cli/saveManager.ts` 顶部加：

```ts
const SAVE_ID_RE = /^save_\d+$/;

function assertValidId(id: string): void {
  if (!SAVE_ID_RE.test(id)) {
    throw new Error(`invalid save id: ${JSON.stringify(id)}`);
  }
}
```

在 `loadGame` 和 `deleteSave` 第一行调用 `assertValidId(id)`。

- [ ] **Step 4: 跑测试**

Run: `npx vitest run src/test/saveManager.security.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/saveManager.security.test.ts src/cli/saveManager.ts
git commit -m "fix(cli/save): whitelist save id with /^save_\\d+$/ to prevent path traversal"
```

---

## Task 12: 测试 helper：`makeGameSim` 工厂

**Files:**
- Create: `src/test/helpers/makeGame.ts`

**Background:** Task 7、8 中都需要构造一个可控初始状态的 `GameSim`。把工厂抽出来方便后续 P1 任务复用。无业务逻辑，可不严格 TDD。

- [ ] **Step 1: 写工厂**

```ts
// src/test/helpers/makeGame.ts
import { GameSim } from '@worker/core/game/GameSim';
import type { TeamGameSim } from '@worker/core/game/types';

export function makeMinimalTeam(tid: number): TeamGameSim {
  // 构造一支足够 GameSim 不崩的最小阵容：每位置 ≥1 人，能力中等。
  // 字段以仓库 TeamGameSim 类型为准；本 helper 在编写时按真实类型填充。
  return /* ... */ as unknown as TeamGameSim;
}

export function makeGame(opts?: {
  scrimmage?: number;
  down?: number;
  toGo?: number;
  quarter?: number;
  clock?: number;
}) {
  const home = makeMinimalTeam(0);
  const away = makeMinimalTeam(1);
  const sim = new GameSim({
    gid: 1,
    season: 2026,
    teams: [home, away],
    // 其它必填字段以仓库构造签名为准
  } as any);
  if (opts?.scrimmage !== undefined) sim.scrimmage = opts.scrimmage;
  if (opts?.down !== undefined) sim.down = opts.down;
  if (opts?.toGo !== undefined) sim.toGo = opts.toGo;
  if (opts?.quarter !== undefined) (sim as any).quarter = opts.quarter;
  if (opts?.clock !== undefined) sim.clock = opts.clock;
  return sim;
}
```

- [ ] **Step 2: 让 Task 7、8 改用此 helper（追加在原 commit 之后或合入新 commit）**

```ts
import { makeGame } from './helpers/makeGame';
const sim = makeGame({ scrimmage: 2, down: 1, toGo: 10, quarter: 1, clock: 15 });
```

- [ ] **Step 3: 跑全量**

Run: `npm test`
Expected: 全绿。

- [ ] **Step 4: Commit**

```bash
git add src/test/helpers/makeGame.ts src/test/gamesim.safety.test.ts src/test/gamesim.turnoverOnDowns.test.ts
git commit -m "test(helpers): add makeGame factory and adopt it in safety/turnover tests"
```

---

## P0 完工检查

- [ ] `npx tsc --noEmit` 退出码 0
- [ ] `npm run lint-js` 退出码 0
- [ ] `npm test` 全绿，新增 ≥ 6 个测试文件
- [ ] 新增测试至少覆盖：timeRemaining、safety×2、turnoverOnDowns、firstDown×3、draftOrder、save security×3
- [ ] `git log --oneline` 看到 12 条 commit，每条独立可回滚

---

# Phase P1：单一权威源 + 工程基线

---

## Task 13: 引入 seedable PRNG

**Files:**
- Modify: `src/common/random.ts`
- Test: `src/test/random.seed.test.ts`（新建）

**Background:** 当前所有随机调用 `Math.random()`，导致比赛不可重放、测试 flaky。引入 mulberry32 + 模块级 seed，保留旧 API 兼容；测试可 `setSeed(...)`。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/random.seed.test.ts
import { describe, it, expect, beforeEach } from 'vitest';
import { setSeed, randInt, choice } from '@common/random';

describe('seeded RNG', () => {
  beforeEach(() => setSeed(42));

  it('reproduces the same sequence given the same seed', () => {
    const a = [randInt(0, 100), randInt(0, 100), randInt(0, 100)];
    setSeed(42);
    const b = [randInt(0, 100), randInt(0, 100), randInt(0, 100)];
    expect(a).toEqual(b);
  });

  it('choice is reproducible too', () => {
    const arr = ['a', 'b', 'c', 'd', 'e'];
    setSeed(7);
    const first = [choice(arr), choice(arr), choice(arr)];
    setSeed(7);
    const second = [choice(arr), choice(arr), choice(arr)];
    expect(first).toEqual(second);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/random.seed.test.ts`
Expected: FAIL（`setSeed` 未导出）。

- [ ] **Step 3: 实现 mulberry32 + 替换 `Math.random` 调用点**

把 `src/common/random.ts` 改为：

```ts
let _state = (Math.random() * 0xffffffff) >>> 0;

function next(): number {
  _state = (_state + 0x6D2B79F5) >>> 0;
  let t = _state;
  t = Math.imul(t ^ (t >>> 15), t | 1);
  t ^= t + Math.imul(t ^ (t >>> 7), t | 61);
  return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
}

export function setSeed(seed: number): void {
  _state = seed >>> 0;
}

export function rand(): number {
  return next();
}

export function random(num: number): number {
  return Math.floor(next() * num);
}

export function randInt(min: number, max: number): number {
  return Math.floor(next() * (max - min + 1)) + min;
}

export function bound(value: number, min: number, max: number): number {
  return Math.max(min, Math.min(max, value));
}

export function coinFlip(): boolean {
  return next() < 0.5;
}

export function truncGauss(mean: number, sd: number, min?: number, max?: number): number {
  const u1 = 1 - next();
  const u2 = 1 - next();
  const z = Math.sqrt(-2 * Math.log(u1)) * Math.cos(2 * Math.PI * u2);
  let result = z * sd + mean;
  if (min !== undefined) result = Math.max(result, min);
  if (max !== undefined) result = Math.min(result, max);
  return Math.round(result);
}

export function choice<T>(array: T[]): T {
  return array[Math.floor(next() * array.length)];
}

export function shuffle<T>(array: T[]): T[] {
  const result = [...array];
  for (let i = result.length - 1; i > 0; i--) {
    const j = Math.floor(next() * (i + 1));
    [result[i], result[j]] = [result[j], result[i]];
  }
  return result;
}

export function sample<T>(array: T[], num?: number): T[] {
  if (!num || num >= array.length) return [...array];
  return shuffle(array).slice(0, num);
}

export function unique<T>(array: T[]): T[] { return Array.from(new Set(array)); }
export function deepCopy<T>(obj: T): T { return JSON.parse(JSON.stringify(obj)); }
export function merge<T extends object>(target: T, source: Partial<T>): T { return { ...target, ...source }; }
```

> **Note:** 业务代码里**仍可能**直接出现 `Math.random()`（如 `GameSim.ts:219, 224, 228, 231, 261, 301, 302`、`pool.ts:25,47,48,49,155,160` 等）。本任务**不**全面替换；这些会出现在后续单独任务（P2 阶段）。但要确保测试里 `setSeed` 至少能控制 `random.ts` 里所有函数的产出。

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/random.seed.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/common/random.ts src/test/random.seed.test.ts
git commit -m "feat(random): add seedable mulberry32 PRNG with setSeed for reproducibility"
```

---

## Task 14: 选定单一赛程实现，删除另一套

**Files:**
- Modify: `src/worker/core/season/index.ts`（barrel 改导出 V2）
- Delete: `src/worker/core/season/seasonManager.ts`（v1）
- Modify: `src/worker/core/league/index.ts:237-312`（删除独立排程，改委托 V2 / 或反之，二选一）

**Background:** `seasonManagerV2` 与 `league/index.ts` 各有一套 schedule 算法，且 barrel 还在 export v1。本任务以 `seasonManagerV2` 为权威，删 v1，让 league 不再自造排程。

- [ ] **Step 1: 把 barrel 切到 V2**

`src/worker/core/season/index.ts`:

```ts
export { SeasonManagerV2 as SeasonManager } from './seasonManagerV2';
export type * from './seasonManagerV2';
```

- [ ] **Step 2: 删 v1**

```bash
git rm src/worker/core/season/seasonManager.ts
```

- [ ] **Step 3: 编译验证**

Run: `npx tsc --noEmit`
Expected: 通过。如果哪里还在 `import { SeasonManager } from '.../seasonManager'`，改为从 barrel 取。

- [ ] **Step 4: 让 league/index.ts 调用 V2 的排程，而不是自造**

定位 `src/worker/core/league/index.ts:237-280`、`283-312` 两段排程，把内部实现替换为：

```ts
import { SeasonManagerV2 } from '../season/seasonManagerV2';
// ...
const sm = new SeasonManagerV2(/* ctor args */);
const schedule = sm.generateRegularSeasonSchedule(/* args */);
return schedule;
```

> **Note:** 具体函数名以 V2 实际公开 API 为准；如缺少公开方法，先在 V2 中暴露一个稳定的 `generateRegularSeasonSchedule(region, teams, season)`，league 只调用该方法。

- [ ] **Step 5: 写一个对赛程平衡性的契约测试**

```ts
// src/test/season.scheduleBalance.test.ts
import { describe, it, expect } from 'vitest';
import { SeasonManagerV2 } from '@worker/core/season/seasonManagerV2';

describe('regular season schedule (closed league)', () => {
  it('每队 17 场，每周 ≤ 半数球队比赛', () => {
    const sm = new SeasonManagerV2(/* ... */);
    const schedule = sm.generateRegularSeasonSchedule('firstContinent', /* 36 teams */, 2026);

    // 每队场次
    const perTeam = new Map<number, number>();
    for (const g of schedule) {
      perTeam.set(g.homeTid, (perTeam.get(g.homeTid) ?? 0) + 1);
      perTeam.set(g.awayTid, (perTeam.get(g.awayTid) ?? 0) + 1);
    }
    for (const [, n] of perTeam) expect(n).toBe(17);

    // 每周比赛数 ≤ 队数 / 2
    const perWeek = new Map<number, number>();
    for (const g of schedule) perWeek.set(g.day, (perWeek.get(g.day) ?? 0) + 1);
    for (const [, n] of perWeek) expect(n).toBeLessThanOrEqual(36 / 2);
  });
});
```

- [ ] **Step 6: 验证**

Run: `npx vitest run src/test/season.scheduleBalance.test.ts && npm test`
Expected: 通过。

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "refactor(season): delete legacy SeasonManager v1, route league schedule through SeasonManagerV2"
```

---

## Task 15: 修矿业岛分级与起源大陆分桶

**Files:**
- Test: `src/test/season.tiering.test.ts`（新建）
- Modify: `src/worker/core/season/seasonManagerV2.ts:186-193`（矿业岛）
- Modify: `src/worker/core/season/seasonManagerV2.ts:256-259`（起源大陆）

**Background:** 矿业岛按数组下标分级；起源大陆按 `did % 3` 分桶。`Team` 实体里应已有显式联赛字段（如 `tier` 或 `league`），改为读字段。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/season.tiering.test.ts
import { describe, it, expect } from 'vitest';
import { SeasonManagerV2 } from '@worker/core/season/seasonManagerV2';

describe('mining island tiering', () => {
  it('groups teams strictly by team.tier (1..4), regardless of array order', () => {
    const teams = /* 80 个 mining 队，故意打乱顺序，每个 tier=1..4 各 20 */;
    const sm = new SeasonManagerV2(/* ... */);
    const schedule = sm.generateRegularSeasonSchedule('miningIsland', teams, 2026);
    // 任一比赛中 home/away 必须同 tier
    for (const g of schedule) {
      const h = teams.find(t => t.tid === g.homeTid)!;
      const a = teams.find(t => t.tid === g.awayTid)!;
      expect(h.tier).toBe(a.tier);
    }
  });
});

describe('origin continent league bucketing', () => {
  it('uses team.league (metropolis/imperial/royal), not team.did % 3', () => {
    const teams = /* 36 个起源队，league 显式设定 */;
    const sm = new SeasonManagerV2(/* ... */);
    const schedule = sm.generateRegularSeasonSchedule('originContinent', teams, 2026);
    // 第一阶段（11 场）必须同 league
    for (const g of schedule.filter(g => g.phase === 'regular-stage1')) {
      const h = teams.find(t => t.tid === g.homeTid)!;
      const a = teams.find(t => t.tid === g.awayTid)!;
      expect(h.league).toBe(a.league);
    }
  });
});
```

> **Note:** 字段名 `tier` / `league` 以仓库 `Team` 实体真实字段为准。如缺，先在 `entities.ts` 上加（数据生成时已隐式分级，只需把它显式化）。

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/season.tiering.test.ts`
Expected: FAIL。

- [ ] **Step 3: 修分级逻辑**

矿业岛：把 186-193 行原本的下标分级改为：

```ts
const tiers = new Map<number, Team[]>();
for (const t of teams) {
  const arr = tiers.get(t.tier) ?? [];
  arr.push(t);
  tiers.set(t.tier, arr);
}
for (const [tier, group] of tiers) {
  // ... 原排程算法对 group 跑一遍
}
```

起源大陆：把 256-259 行 `team.did % 3` 改为：

```ts
const buckets: Record<'metropolis' | 'imperial' | 'royal', Team[]> = {
  metropolis: [], imperial: [], royal: [],
};
for (const t of teams) buckets[t.league].push(t);
```

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/season.tiering.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/season.tiering.test.ts src/worker/core/season/seasonManagerV2.ts
git commit -m "fix(season): tier mining island by team.tier, bucket origin continent by team.league"
```

---

## Task 16: 修升降级按级别分组

**Files:**
- Test: `src/test/offseason.promotionRelegation.test.ts`（新建）
- Modify: `src/worker/core/season/offseason.ts:428-487`

**Background:** 当前对全矿业球队按 `won` 排序，"末尾降级"。设计是**每级末位 3 队降级、下级前 3 队升级**。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/offseason.promotionRelegation.test.ts
import { describe, it, expect } from 'vitest';
import { applyPromotionRelegation } from '@worker/core/season/offseason';

describe('mining island promotion/relegation', () => {
  it('relegates bottom 3 of each tier and promotes top 3 of the tier below', () => {
    const teams = [
      // tier 1 (super league): 20 队，最差 3 队 won = 0..2
      ...mkTier(1, 20),
      // tier 2: 20 队，最强 3 队 won = 17..19
      ...mkTier(2, 20),
      ...mkTier(3, 20),
      ...mkTier(4, 20),
    ];
    const after = applyPromotionRelegation(teams);

    const tier1 = after.filter(t => t.tier === 1).sort((a, b) => a.tid - b.tid);
    const tier2 = after.filter(t => t.tier === 2).sort((a, b) => a.tid - b.tid);
    expect(tier1.length).toBe(20);
    expect(tier2.length).toBe(20);
    // 上一年 tier2 的 top 3 现在应在 tier 1
    // 上一年 tier1 的 bottom 3 现在应在 tier 2
    // ...
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/offseason.promotionRelegation.test.ts`
Expected: FAIL。

- [ ] **Step 3: 修复**

把 `src/worker/core/season/offseason.ts:428-487` 中的整体排序删掉，改为按 `tier` 分组后处理：对每对相邻 tier，相互交换末/首 3 队的 `tier` 字段。

```ts
const N_PROMO = 3;
const TIERS = [1, 2, 3, 4] as const;

for (let i = 0; i < TIERS.length - 1; i++) {
  const upper = teams.filter(t => t.tier === TIERS[i]).sort((a, b) => a.won - b.won);
  const lower = teams.filter(t => t.tier === TIERS[i + 1]).sort((a, b) => b.won - a.won);
  for (let k = 0; k < N_PROMO; k++) {
    upper[k].tier = TIERS[i + 1];
    lower[k].tier = TIERS[i];
  }
}
```

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/offseason.promotionRelegation.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/offseason.promotionRelegation.test.ts src/worker/core/season/offseason.ts
git commit -m "fix(offseason): promote/relegate within adjacent tiers (3 up, 3 down)"
```

---

## Task 17: 完成双败淘汰下半部 + 拒绝幽灵队

**Files:**
- Test: `src/test/playoffs.doubleElim.test.ts`（新建）
- Modify: `src/worker/core/playoffs/index.ts:60-71, 152-207`

**Background:** 当前双败淘汰只生成首轮胜者组；不足 12 队用 `tid: -1` 占位。本任务：①不足资格直接抛错或缩短括号；②补全败者组与决赛对阵生成与晋级。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/playoffs.doubleElim.test.ts
import { describe, it, expect } from 'vitest';
import { generateDoubleEliminationBracket, advanceDoubleEliminationRound }
  from '@worker/core/playoffs';

describe('double elimination bracket', () => {
  it('throws when given fewer than 8 qualified teams', () => {
    expect(() => generateDoubleEliminationBracket(/* 5 teams */, 'originContinent', 2026))
      .toThrow(/at least 8/);
  });

  it('generates losers bracket rounds equal to log2(N) for N power of 2', () => {
    const bracket = generateDoubleEliminationBracket(/* 8 teams */, 'originContinent', 2026);
    expect(bracket.winnersBracket.length).toBeGreaterThan(0);
    expect(bracket.losersBracket.length).toBeGreaterThan(0);
  });

  it('produces a champion after fully advancing', () => {
    let bracket = generateDoubleEliminationBracket(/* 8 teams */, 'originContinent', 2026);
    while (bracket.champion === undefined) {
      bracket = advanceDoubleEliminationRound(bracket);
    }
    expect(bracket.champion).toBeGreaterThanOrEqual(0);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/playoffs.doubleElim.test.ts`
Expected: FAIL。

- [ ] **Step 3: 实现**

`src/worker/core/playoffs/index.ts`:

```ts
const MIN_DOUBLE_ELIM_TEAMS = 8;

export function generateDoubleEliminationBracket(
  teams: Team[], region: string, year: number,
): DoubleEliminationBracket {
  if (teams.length < MIN_DOUBLE_ELIM_TEAMS) {
    throw new Error(`double elimination requires at least ${MIN_DOUBLE_ELIM_TEAMS} teams (got ${teams.length})`);
  }
  const seeded = teams.slice(0, 8); // 真实场景按战绩种子
  const winnersR1 = pairSeeds(seeded);
  return {
    region, year, teams: seeded,
    winnersBracket: [{ roundType: 'winners', round: 1, matchups: winnersR1 }],
    losersBracket: [],
    currentRound: 1,
  };
}

export function advanceDoubleEliminationRound(b: DoubleEliminationBracket)
  : DoubleEliminationBracket {
  // 1) 模拟 winners 当前轮 -> 产生 winners 下一轮 + losers 进入下一轮 losers
  // 2) 模拟 losers 当前轮 -> 产生 losers 下一轮
  // 3) 当 winners.length === 1 && losers.length === 1 -> 决赛
  // 4) 决赛胜者为冠军（loser 若先输过则需双冠军决赛）
  // ...具体实现按 NFL/MLB 双败规则
}

function pairSeeds(seeded: Team[]): PlayoffMatchup[] {
  // 1v8, 4v5, 2v7, 3v6 等标准种子配对
  // ...
}
```

> **Note:** 双败淘汰规则细节较多，此处给框架；具体实现以 `winners.length`/`losers.length` 在每轮的同步推进为主，决赛逻辑（败者组冠军赢一场需再赢一场才夺冠）需明确建模。

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/playoffs.doubleElim.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/playoffs.doubleElim.test.ts src/worker/core/playoffs/index.ts
git commit -m "feat(playoffs): complete double-elimination bracket with losers bracket and championship"
```

---

## Task 18: 修休赛期 `bornYear` 与年龄双重推移

**Files:**
- Test: `src/test/offseason.aging.test.ts`（新建）
- Modify: `src/worker/core/season/offseason.ts:131-137`

**Background:** 同时 `age += 1` 和 `bornYear -= 1`，等于年龄被推 2 年。`bornYear` 应该固定，`age` 由 `season - bornYear` 派生；或反之。这里采用：**`bornYear` 固定，年龄派生**。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/offseason.aging.test.ts
import { describe, it, expect } from 'vitest';
import { ageOnePlayer } from '@worker/core/season/offseason';

describe('player aging in offseason', () => {
  it('keeps bornYear stable; age is derived', () => {
    const p = { age: 25, bornYear: 2001, /* ... */ } as any;
    const after = ageOnePlayer(p, /*newSeason=*/ 2027);
    expect(after.bornYear).toBe(2001);
    expect(after.age).toBe(26);
  });

  it('does not double-shift across multiple seasons', () => {
    let p: any = { age: 22, bornYear: 2004 };
    for (let s = 2027; s <= 2030; s++) p = ageOnePlayer(p, s);
    expect(p.bornYear).toBe(2004);
    expect(p.age).toBe(26);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/offseason.aging.test.ts`
Expected: FAIL。

- [ ] **Step 3: 修复**

把 `src/worker/core/season/offseason.ts:131-137` 改为：

```ts
export function ageOnePlayer<T extends { age: number; bornYear: number }>(p: T, newSeason: number): T {
  return { ...p, age: newSeason - p.bornYear };
}
```

把休赛期内调用处替换为 `ageOnePlayer(p, season + 1)`。

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/offseason.aging.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/offseason.aging.test.ts src/worker/core/season/offseason.ts
git commit -m "fix(offseason): age players via bornYear, do not shift bornYear"
```

---

## Task 19: 交易估值用游戏赛季年，不用现实日历年

**Files:**
- Test: `src/test/trade.evaluate.test.ts`（新建）
- Modify: `src/worker/core/trade/evaluate.ts:24-29` 及函数签名

**Background:** `new Date().getFullYear()` 让 2026 年现实玩 2025 赛季会算错合同剩余年。函数应接受 `gameSeason` 参数。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/trade.evaluate.test.ts
import { describe, it, expect } from 'vitest';
import { evaluatePlayerValue } from '@worker/core/trade/evaluate';

const player = {
  pid: 1,
  ovr: 80,
  age: 27,
  contract: { amount: 5_000_000, exp: 2030, years: 4, /* ... */ },
} as any;

describe('evaluatePlayerValue', () => {
  it('uses gameSeason for years remaining, not real calendar year', () => {
    const v2026 = evaluatePlayerValue(player, /*gameSeason=*/ 2026);
    const v2029 = evaluatePlayerValue(player, /*gameSeason=*/ 2029);
    expect(v2026).toBeGreaterThan(v2029);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/trade.evaluate.test.ts`
Expected: FAIL（参数缺失或两值相等）。

- [ ] **Step 3: 修签名**

把所有交易估值相关函数加 `gameSeason: number` 参数；删除 `new Date().getFullYear()` 调用。所有调用方（grep `evaluatePlayerValue\\|evaluateTrade`）传入 `engine.season` / `state.season`。

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/trade.evaluate.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/trade.evaluate.test.ts src/worker/core/trade/evaluate.ts $(rg -l 'evaluatePlayerValue\\|evaluateTrade' src --type ts)
git commit -m "fix(trade): pass gameSeason explicitly; do not depend on real-world Date"
```

---

## Task 20: `StatsManager.finalizeGame` 不再硬编码 2025

**Files:**
- Test: `src/test/stats.finalize.test.ts`（新建）
- Modify: `src/worker/core/game/GameSim.ts:932-936`

**Background:** `finalizeGame` 把 `season: 2025` 写死，`this.season` 被忽略。

- [ ] **Step 1: 写失败测试**

```ts
// src/test/stats.finalize.test.ts
import { describe, it, expect } from 'vitest';
import { makeGame } from './helpers/makeGame';

describe('GameSim.finalizeGame', () => {
  it('writes the actual game season into stats, not hardcoded 2025', () => {
    const sim = makeGame({});
    (sim as any).season = 2030;
    sim.run();
    const out = sim.getResult(); // 或 finalizeGame 直接返回的对象
    expect(out.season).toBe(2030);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run src/test/stats.finalize.test.ts`
Expected: FAIL。

- [ ] **Step 3: 修复**

把 `src/worker/core/game/GameSim.ts:932-936` 中的 `season: 2025` 改成 `season: this.season`。

- [ ] **Step 4: 验证**

Run: `npx vitest run src/test/stats.finalize.test.ts && npm test`
Expected: 通过。

- [ ] **Step 5: Commit**

```bash
git add src/test/stats.finalize.test.ts src/worker/core/game/GameSim.ts
git commit -m "fix(gamesim): use this.season in finalizeGame instead of hardcoded 2025"
```

---

## Task 21: 存档 schema + 版本号校验

**Files:**
- Test: `src/test/saveManager.schema.test.ts`（新建）
- Modify: `src/cli/saveManager.ts`

**Background:** `JSON.parse` 后只 `as SaveGame`，损坏档/旧档静默崩。引入 zod schema + `schemaVersion` 字段；读到不兼容版本走 `migrate()`（先返回 `null` + 报告）。

- [ ] **Step 1: 安装 zod**

Run: `npm i zod`

- [ ] **Step 2: 写失败测试**

```ts
// src/test/saveManager.schema.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as fs from 'fs';
import { loadGame, saveGame, CURRENT_SAVE_SCHEMA_VERSION } from '../cli/saveManager';

describe('save schema validation', () => {
  it('rejects malformed JSON gracefully', () => {
    vi.spyOn(fs, 'existsSync').mockReturnValue(true);
    vi.spyOn(fs, 'readFileSync').mockReturnValue('{ not json');
    expect(loadGame('save_1')).toBeNull();
  });

  it('rejects save with missing required fields', () => {
    vi.spyOn(fs, 'existsSync').mockReturnValue(true);
    vi.spyOn(fs, 'readFileSync').mockReturnValue('{"id":"save_1"}');
    expect(loadGame('save_1')).toBeNull();
  });

  it('writes schemaVersion when saving', () => {
    let written = '';
    vi.spyOn(fs, 'writeFileSync').mockImplementation((_, data) => { written = String(data); });
    vi.spyOn(fs, 'existsSync').mockReturnValue(true);
    saveGame('test', /* valid SaveData */ as any);
    expect(JSON.parse(written).schemaVersion).toBe(CURRENT_SAVE_SCHEMA_VERSION);
  });
});
```

- [ ] **Step 3: 跑测试确认失败**

Run: `npx vitest run src/test/saveManager.schema.test.ts`
Expected: FAIL。

- [ ] **Step 4: 改 saveManager**

```ts
import { z } from 'zod';

export const CURRENT_SAVE_SCHEMA_VERSION = 1;

const SaveDataSchema = z.object({
  teams: z.array(z.any()),
  players: z.array(z.any()),
  freeAgents: z.array(z.any()).optional(),
  seasonYear: z.number(),
  currentWeek: z.number(),
  schedule: z.array(z.any()),
  standings: z.array(z.any()),
  userTeamTid: z.number(),
});

const SaveGameSchema = z.object({
  schemaVersion: z.literal(CURRENT_SAVE_SCHEMA_VERSION),
  id: z.string().regex(/^save_\d+$/),
  name: z.string(),
  timestamp: z.number(),
  seasonYear: z.number(),
  currentWeek: z.number(),
  userTeamName: z.string(),
  userTeamRegion: z.string(),
  data: SaveDataSchema,
});

export type SaveGame = z.infer<typeof SaveGameSchema>;
```

`loadGame` 内：

```ts
try {
  const parsed = JSON.parse(content);
  const result = SaveGameSchema.safeParse(parsed);
  if (!result.success) {
    console.error(`Invalid save ${id}:`, result.error.issues);
    return null;
  }
  return result.data;
} catch (e) {
  console.error(`Error loading save ${id}:`, e);
  return null;
}
```

`saveGame` 内：写入对象时加 `schemaVersion: CURRENT_SAVE_SCHEMA_VERSION`。

- [ ] **Step 5: 验证**

Run: `npx vitest run src/test/saveManager.schema.test.ts && npm test`
Expected: 通过。

- [ ] **Step 6: Commit**

```bash
git add src/test/saveManager.schema.test.ts src/cli/saveManager.ts package.json package-lock.json
git commit -m "feat(save): zod schema + schemaVersion for save/load with safe failure"
```

---

## Task 22: `worker/api` 收口 — 禁止 UI/CLI 直接 import worker/core

**Files:**
- Modify: `src/worker/api/GameEngine.ts`（暴露所需 API）
- Modify: `src/worker/api/index.ts`（barrel）
- Modify: 多个 UI 组件（删除 `@worker/core/...` 直接 import）
- Modify: `src/cli/main.ts`
- Modify: `eslint.config.js`（加 `no-restricted-imports` 规则）

**Background:** UI 组件大量 `import @worker/core/...`，绕过 API 抽象层。本任务把所需能力收拢到 `GameEngine` / `worker/api` 上，并用 ESLint 规则禁止反向依赖。

- [ ] **Step 1: 列出所有违规 import**

Run: `rg "@worker/core" src/ui src/cli --type ts --type tsx -n`
Expected: 一份清单（GameSimView、TradeCenter、ImperialCupView、DraftRoom、FreeAgencyView、PlayoffsView、StatsView、cli/main.ts 等）。

- [ ] **Step 2: 在 `worker/api` 上扩展 API**

按清单总结需要的方法（如 `simulateOneGameDetail(gid)`、`getStandings()`、`evaluateTradeProposal(...)`、`getDraftPool()`、`getOriginDraftEligible()` 等），都加在 `GameEngine` 上并通过 `worker/api/index.ts` 导出。每加一个方法一个 commit。

- [ ] **Step 3: 替换违规 import**

把每个组件的 `import { X } from '@worker/core/...'` 替换为通过 `getGameEngine().xxx()` 或 `import type { X } from '@worker/api/types'`。

- [ ] **Step 4: 加 ESLint 规则禁止反向依赖**

在 `eslint.config.js` 中加：

```js
{
  files: ['src/ui/**/*.{ts,tsx}', 'src/cli/**/*.ts'],
  rules: {
    'no-restricted-imports': ['error', {
      patterns: [{
        group: ['@worker/core/*', '@worker/db/*'],
        message: 'UI/CLI must go through @worker/api, not internal modules.',
      }],
    }],
  },
},
```

- [ ] **Step 5: 验证**

Run: `npm run lint-js && npx tsc --noEmit && npm test`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "refactor(api): consolidate worker/api surface; forbid UI/CLI from importing worker/core"
```

---

## Task 23: 把完整 `GameSim` 真正搬到 Web Worker

**Files:**
- Modify: `src/ui/workers/generateWorker.ts`（或新建 `src/ui/workers/simWorker.ts`）
- Modify: `src/ui/components/GameSimView.tsx`
- Modify: `vite.config.ts`（如需 worker 配置）

**Background:** `GameSim` 当前在浏览器主线程跑，`setTimeout` + `rAF` 步进。把它搬到 Web Worker，用 `postMessage` 流式传 play-by-play。

- [ ] **Step 1: 设计 worker 协议**

```ts
// src/ui/workers/simWorker.protocol.ts
export type SimRequest =
  | { type: 'simulate'; gid: number; teams: TeamGameSim[]; season: number; seed: number };

export type SimResponse =
  | { type: 'event'; gid: number; event: GameEvent }
  | { type: 'done'; gid: number; result: GameResult }
  | { type: 'error'; gid: number; message: string };
```

- [ ] **Step 2: 写 worker 文件**

```ts
// src/ui/workers/simWorker.ts
/// <reference lib="webworker" />
import { GameSim } from '@worker/core/game/GameSim';
import { setSeed } from '@common/random';
import type { SimRequest, SimResponse } from './simWorker.protocol';

self.onmessage = (e: MessageEvent<SimRequest>) => {
  const msg = e.data;
  try {
    if (msg.type === 'simulate') {
      setSeed(msg.seed);
      const sim = new GameSim({ gid: msg.gid, season: msg.season, teams: msg.teams } as any);
      sim.onEvent = (event) => {
        (self as any).postMessage({ type: 'event', gid: msg.gid, event } as SimResponse);
      };
      const result = sim.run();
      (self as any).postMessage({ type: 'done', gid: msg.gid, result } as SimResponse);
    }
  } catch (err) {
    (self as any).postMessage({
      type: 'error', gid: msg.gid,
      message: err instanceof Error ? err.message : String(err),
    } as SimResponse);
  }
};
```

> **Note:** `GameSim` 当前未必有 `onEvent` 回调；如缺，本任务**最小**地加一个回调字段（可选），不动其它逻辑。

- [ ] **Step 3: 在 `GameSimView` 用 Vite 的 `?worker` 导入**

```tsx
import SimWorker from '../workers/simWorker?worker';

// ...
const workerRef = useRef<Worker | null>(null);
useEffect(() => {
  const w = new SimWorker();
  workerRef.current = w;
  w.onmessage = (e: MessageEvent<SimResponse>) => {
    if (e.data.type === 'event') appendPlay(e.data.event);
    else if (e.data.type === 'done') setResult(e.data.result);
    else if (e.data.type === 'error') setError(e.data.message);
  };
  return () => w.terminate();
}, []);

const startSim = () => {
  workerRef.current?.postMessage({
    type: 'simulate', gid, teams, season, seed: Date.now(),
  } satisfies SimRequest);
};
```

- [ ] **Step 4: 删除主线程的 `setTimeout`/`rAF` 步进逻辑**

- [ ] **Step 5: 跑端到端**

```bash
npm run build && npm test
```

Expected: 构建通过、测试通过。手动 `npm run dev` 打开比赛页面，确认主线程不再卡顿（不强制写自动化检测）。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(ui): run GameSim in a Web Worker, stream events via postMessage"
```

---

## Task 24: 删 `seasonManager.ts` 之外的死代码 + 未用依赖

**Files:**
- Delete: `src/ui/workers/generateWorker.ts`（如确认无引用，已被 simWorker 替代）
- Modify: `package.json`（移除 `commander` 等未用依赖）
- Modify: `src/cli/main.ts`（删除未使用的 `GameSim` import）

**Background:** 收尾。

- [ ] **Step 1: 验证 generateWorker.ts 无引用**

Run: `rg "generateWorker" src --type ts --type tsx`
Expected: 仅自身。

- [ ] **Step 2: 删文件**

```bash
git rm src/ui/workers/generateWorker.ts
```

- [ ] **Step 3: 删未用依赖**

Run: `rg "from 'commander'\\|require\\('commander'\\)" src --type ts`
Expected: 无。然后：

```bash
npm uninstall commander
```

- [ ] **Step 4: 删 cli/main.ts 顶部未用的 GameSim import**

- [ ] **Step 5: 验证**

Run: `npx tsc --noEmit && npm run lint-js && npm test`
Expected: 全绿。

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "chore: remove dead generateWorker, unused commander dep, and unused GameSim import in cli"
```

---

## Task 25: P1 集成测试 — 一个完整赛季能从 init 走到 offseason

**Files:**
- Create: `src/test/integration.fullSeason.test.ts`

**Background:** 最后一个 task：跑一遍完整集成路径，确保没有引入回归。

- [ ] **Step 1: 写测试**

```ts
// src/test/integration.fullSeason.test.ts
import { describe, it, expect } from 'vitest';
import { setSeed } from '@common/random';
import { GameEngine } from '@worker/api/GameEngine';

describe('integration: full season run', () => {
  it('initializes a league, simulates 17 weeks, runs playoffs, and enters offseason', async () => {
    setSeed(2026);
    const engine = new GameEngine();
    await engine.initLeague({ region: 'firstContinent', userTeamTid: 0, season: 2026 } as any);

    for (let w = 1; w <= 17; w++) {
      await engine.simulateWeek();
    }

    expect(engine.getStandings().length).toBeGreaterThan(0);

    await engine.runPlayoffs();
    expect(engine.getPlayoffChampion()).toBeDefined();

    await engine.runOffseason();
    expect(engine.state.season).toBe(2027);
  });
});
```

> **Note:** 各 API 名以 `GameEngine` 实际方法为准；缺则在本任务前置补齐。

- [ ] **Step 2: 跑测试**

Run: `npx vitest run src/test/integration.fullSeason.test.ts`
Expected: PASS（如有 fail，回到对应 P1 task 修补；不要在本 task 内偷修业务逻辑）。

- [ ] **Step 3: Commit**

```bash
git add src/test/integration.fullSeason.test.ts
git commit -m "test(integration): full-season smoke test (init -> regular -> playoffs -> offseason)"
```

---

## P1 完工检查

- [ ] `npx tsc --noEmit` 退出码 0
- [ ] `npm run lint-js` 退出码 0
- [ ] `npm test` 全绿
- [ ] 所有新增测试 ≥ 25 条 case
- [ ] `rg "@worker/core" src/ui src/cli --type ts --type tsx` 输出为空
- [ ] `rg "Math\\.random\\(\\)" src/common/random.ts` 输出为空（PRNG 已替换）
- [ ] `git log --oneline fix/p0-p1-recovery` 显示干净的 25 个 commit

---

## 成功度量（与"全项目审查报告"严重问题对应）

| 报告 | 对应 Task |
|---|---|
| A1 StatsManager TS2305 | Task 1 |
| A2 common barrel TS2308 | Task 2 |
| A3 DraftPick 双源 | Task 3 |
| A4 ESLint 缺配置 | Task 5 |
| A5 tools/ 缺失 | Task 5 |
| B1 timeRemaining | Task 6 |
| B2 doPass/doRun safety 漏记 | Task 7 |
| B3 第四档不完成 | Task 8 |
| B4 penalties firstDown | Task 9 |
| B5 finalizeGame 硬编码 2025 | Task 20 |
| C1 赛程双轨 | Task 14 |
| C2 矿业岛分级 | Task 15 |
| C3 起源 did % 3 | Task 15 |
| C4 双败淘汰下半部 | Task 17 |
| C5 幽灵队占位 | Task 17 |
| C6 升降级未按级 | Task 16 |
| D1 选秀 won:0 | Task 10 |
| D2 trade 用 real year | Task 19 |
| E1 路径穿越 | Task 11 |
| E2 存档无 schema | Task 21 |
| F3 GameSim 主线程 | Task 23 |
| F5 UI 绕过 API | Task 22 |
| `random.ts` 不可重放 | Task 13 |
| 死代码（v1 SeasonManager / generateWorker / commander） | Task 14, Task 24 |

未覆盖（留待 P2）：B6 `passRushers` 空数组 NaN、伤病不影响出战能力、GameSim 缺 2PT/抄截 TD/防守得分、加时规则简化、`gameStore` re-render 治理、`ScheduleView` useMemo deps、`generate.ts` 与 `ovr.ts` 权重不一致、`StatsManager` 单例状态隔离、`storage.ts` 写死 `get(2025)`、依赖大版本升级与可疑老依赖（`confetti-js` 等）。

---

## 自检（writing-plans skill 要求）

- **Spec 覆盖**：上表逐条对应。未覆盖项已显式标注 P2，避免把 P1 撑爆。
- **Placeholder 扫描**：没有 "TBD"、"implement later"；少数地方写了 `/* ... */`（专指仓库实际签名/字段需在执行时核对的部分），均为**Note** 注解，不是步骤主体。
- **类型一致性**：`SaveGameSchema` 与 `SaveGame` 使用 `z.infer`；`DraftPick` 全程指向 `@common/types`；`ageOnePlayer` 通过泛型保持字段稳定；`SimRequest`/`SimResponse` 在 protocol 文件单一定义。

---

# 执行交接

**计划已保存到 `docs/plans/2026-04-19-sifi-nfl-p0-p1.md`。两种执行选项：**

**1. Subagent-Driven（推荐）** — 每个 Task 派一个新 subagent 执行，Task 之间做 review，便于快速迭代，每次 subagent 不带历史上下文，靠本计划文件即可工作。

**2. Inline Execution** — 在当前会话内连续执行，每完成 N 个 Task 做一次 checkpoint review。

**你选哪种？或者也可以"先只跑 P0（Task 1–12），P1 之后再说"。**
